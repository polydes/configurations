package com.polydes.configurations;

/**
 * A version of DropdownConverter that saves strings from a set by hashing them.
 * 
 * To read back to the original string, the list of all original strings must be
 * available so we can match the hashes.
 * 
 * As long as the strings remain consistent, this allows us to use different sets
 * of strings
 */
public class StringDropdownConverter
{
	
}
